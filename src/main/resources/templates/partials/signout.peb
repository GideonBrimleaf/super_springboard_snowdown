<form class="bg-red-400 flex" method="post" action="/logout">
  <input type="hidden" name="{{request.getAttribute('_csrf').parameterName}}" value="{{request.getAttribute('_csrf').token}}" />
  <button class="ml-auto mr-6 text-white text-right" id="logout-button" type="submit">Logout</button>
    <p>The thing is {{ request }}</p>
</form>