{% extends 'templates/app.peb' %}

{% block content %}
  <main class="mx-10 my-5">
    <h1>{{ snowboarder.name }}</h1>
    <p>{{ snowboarder.age }}</p>
    <img src="{{ snowboarder.profile }}" alt="{{ snowboarder.name }}'s profile pic">
  </main>
{% endblock %}